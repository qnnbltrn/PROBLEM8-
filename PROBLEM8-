#include <iostream>
#include <conio.h>

using namespace std;

int main()
{
  int q;
  
  cout << "Enter the value of a positive integer:";
  cin >> q;
  
  for (int a = 1; a <= 10; ++a)
  {
      cout << q << "*" << a << "=" << q*a << endl;
  }

_getch ();
return 0;
}
